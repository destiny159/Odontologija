<form method="post" action="{{ path('atsiliepimai_delete', {'id': atsiliepimai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ atsiliepimai.id) }}">
    <button class="btn">Pašalinti</button>
</form>
